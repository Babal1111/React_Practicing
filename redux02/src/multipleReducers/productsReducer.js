import { productsList } from "../../../redux/src/productsList";




export default function productReducer(state = productsList,action){ //reducer take 2 params prevs state and action


return state;
}